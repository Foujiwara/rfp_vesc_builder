# =====================================
# RFP VESC Firmware Builder Configuration
# =====================================

# Upstream selection:
#   release_6_02 → fixed stable
#   release_6_05 → fixed stable
#   release_6_06 → fixed stable
#   latest       → latest official release
#   master       → upstream master (BETA)

# Build mode: all | single
BUILD_MODE="single"

# Used only if BUILD_MODE="single"
SINGLE_MODEL="C350_V4_R2"

# Build also _no_limits variants: yes | no
BUILD_NO_LIMITS="yes"

# Prefix firmware name and HW_NAME with RFP_: yes | no
PREFIX_RFP="yes"

# Upstream firmware tag / branch (single source of truth)
TAG="release_6_06"
